#include "db_date.h"
#include "object_pool.h"

int init()
{
	/*
	int ret = 0;
	ret = object_pool<db_date>::instance()->init(100);
	if (ret < 0)
		goto error;
	return 0;

error:
	return -1;
	*/
	return 0;
}
